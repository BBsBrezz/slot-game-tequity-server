# 🎰 Slot Game Tequity 平台啟動指南

## 📋 系統需求

- Node.js (建議 v18 或以上版本)
- npm 或 yarn
- 終端機 (Terminal/Command Line)
- 網頁瀏覽器

## 🚀 啟動步驟

### 🔧 Server 端啟動步驟

#### 1. 進入服務器目錄
```bash
cd /slot-game-tequity-server
```

#### 2. 安裝依賴包
```bash
npm install
```

#### 3. 編譯 TypeScript 代碼
```bash
npm run build
```

#### 4. 選擇啟動方式

**方式一：Mock API Server (推薦用於測試)**
```bash
node mock-api-server.js
```
- 端口：8080
- 適用於：快速測試和開發
- 特點：簡化的 API 實現

**方式二：Slotify GDK Server (正式環境)**
```bash
npm start
```
- 端口：8080
- 適用於：正式 Tequity 平台整合
- 特點：完整的 Slotify GDK 功能

#### 5. 確認服務器啟動
看到以下訊息表示成功：

**Mock API Server:**
```
🎰 Mock Slot Game API Server running on port 8080
📋 Available endpoints:
   GET  http://localhost:8080/
   POST http://localhost:8080/authenticate
   POST http://localhost:8080/info
   POST http://localhost:8080/play
```

**Slotify GDK Server:**
```
info: Creating service games!
info: Service listening on port 8080!
```

---

### 🎮 Client 端啟動步驟

#### 1. 進入客戶端目錄
```bash
cd /Users/harry.lu/Downloads/slot-game-tequity-client
```

#### 2. 安裝依賴包
```bash
npm install
```

#### 3. 啟動靜態服務器
```bash
npm start
```

#### 4. 確認客戶端啟動
看到以下訊息表示成功：
```
serving "dist" at http://127.0.0.1:3004
```

#### 5. 開啟遊戲
在瀏覽器中訪問：
```
http://127.0.0.1:3004
```

---

## 🔍 完整啟動流程

### Step 1: 啟動服務器
1. 開啟第一個終端視窗
2. 執行服務器啟動步驟
3. 確認服務器在 port 8080 運行

### Step 2: 啟動客戶端
1. 開啟第二個終端視窗
2. 執行客戶端啟動步驟
3. 確認客戶端在 port 3004 運行

### Step 3: 遊戲測試
1. 開啟瀏覽器
2. 訪問 `http://127.0.0.1:3004`
3. 等待遊戲連線成功
4. 選擇下注金額並開始遊戲

---

## ⚠️ 常見問題解決

### 問題 1: Port 8080 被占用
```bash
# 查看占用進程
lsof -i :8080

# 關閉占用進程
kill -9 [PID]
```

### 問題 2: 依賴包安裝失敗
```bash
# 清除 node_modules 重新安裝
rm -rf node_modules package-lock.json
npm install
```

### 問題 3: TypeScript 編譯錯誤
```bash
# 清除編譯結果重新編譯
npm run build
```

### 問題 4: 客戶端無法連接服務器
- 確認服務器已正常啟動
- 檢查防火牆設定
- 確認端口 8080 和 3004 沒有被其他程式占用

---

## 📊 服務狀態檢查

### 檢查服務器狀態
```bash
curl -X GET http://localhost:8080/
```

### 檢查客戶端狀態
在瀏覽器訪問：`http://127.0.0.1:3004`

---

## 🎯 遊戲說明

### 符號對應
- 🍀 SYM1 (萬能符號)
- 🍎 SYM2
- 🍊 SYM3
- 🍇 SYM4
- 🍓 SYM5
- 🥝 SYM6

### 贏法規則
1. **三個相同符號** (除 🍀) → 2倍下注
2. **萬能組合** (兩種不同符號 + 🍀) → 2倍下注

### 下注選項
- 可選金額：$1, $2, $5, $10, $20, $50, $100, $200
- 默認下注：$5
- 最大獲勝：$10,000

---

## 🛠️ 開發命令

### Server 端
```bash
npm run start:dev    # 開發模式 (TypeScript)
npm run build        # 編譯 TypeScript
npm run lint         # 代碼檢查
npm run test         # 執行測試
```

### Client 端
```bash
npm start           # 啟動靜態服務器
npm run dev         # 開發模式 (同 start)
```

---

*🎲 享受您的老虎機遊戲體驗！*
